# SLM ratio sweep — k=0.7 (keep top 70% tokens by loss)
# Phase 2: find optimal SLM ratio for 17M scale
export MAX_WALLCLOCK_SECONDS=600
export RUN_ID=slm_sweep_k70
export VAL_LOSS_EVERY=200
export TRAIN_LOG_EVERY=100
export GATED_ATTN=none
export ACTIVATION=relu2
export VOCAB_SIZE=1024
export TTT_MODE=none
export SLM_ENABLED=1
export SLM_RATIO=0.7
export QUANT_MODE=int8_zlib
