# V2 C6 + Small Batch (no grad accum) — Paper #15 test
# Remove gradient accumulation: 4x smaller effective batch, 4x more updates
# Must also reduce TRAIN_BATCH_TOKENS to keep per-microstep memory same
export GRAD_ACCUM_STEPS=1
export TRAIN_BATCH_TOKENS=196608
export PERI_LN=0
