# V2 Full Stack — Small Batch + EMA=0.990 + PreQuantTTT
# Combines: B2 (Paper #15) + R3 (EMA) + R4 (PreQuantTTT)
# Base: C6 (headwise + emb7+eclip15)

# --- Small Batch (Paper #15) ---
export GRAD_ACCUM_STEPS=1
export TRAIN_BATCH_TOKENS=196608

# --- EMA deeper averaging ---
export EMA_DECAY=0.990

# --- PreQuantTTT ---
export PREQUANT_TTT_ENABLED=1
export PREQUANT_TTT_EPOCHS=21
export PREQUANT_TTT_LR=5e-4
export PREQUANT_TTT_LR_END=5e-5
