# LeakyReLU² + headwise gated attention + SLM k=0.6
# Triple combo: best activation + best arch + SLM
export MAX_WALLCLOCK_SECONDS=600
export RUN_ID=leaky_relu2_headwise_slm_k60
export VAL_LOSS_EVERY=200
export TRAIN_LOG_EVERY=100
export ACTIVATION=leaky_relu2
export GATED_ATTN=headwise
export VOCAB_SIZE=1024
export TTT_MODE=none
export SLM_ENABLED=1
export SLM_RATIO=0.6
export QUANT_MODE=int8_zlib
