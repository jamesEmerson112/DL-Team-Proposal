# SLM k=0.95 — barely selective, drop only easiest 5% of tokens
# Plain GQA baseline (SP1024, no other enhancements)
# Compare against Run 6v2 (1.2649 BPB, same config without SLM)
export MAX_WALLCLOCK_SECONDS=600
export RUN_ID=slm_sweep_k95_2gpu
export VAL_LOSS_EVERY=200
export TRAIN_LOG_EVERY=100
export GATED_ATTN=none
export ACTIVATION=relu2
export VOCAB_SIZE=1024
export TTT_MODE=none
export SLM_ENABLED=1
export SLM_RATIO=0.95
