# V2 C6 + Structured FFN (rank=0.75, blocks=8) — Paper #5 test
# More conservative: higher rank ratio, more blocks
# ~41% fewer MLP params, moderate throughput gain
export STRUCTURED_FFN=1
export FFN_RANK_RATIO=0.75
export FFN_NUM_BLOCKS=8
export LR_WARMUP_FRAC=0.0
