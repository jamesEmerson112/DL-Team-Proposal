# TEMPLATE — Competition mode — 8 GPUs, 10 min, official PG constraints
# No run ID — base template, source other configs on top
export MAX_WALLCLOCK_SECONDS=600
export RUN_ID=competition_8gpu
export VAL_LOSS_EVERY=1000
export TRAIN_LOG_EVERY=200
export GATED_ATTN=none
export ACTIVATION=relu2
export VOCAB_SIZE=1024
export TTT_MODE=none
export SLM_ENABLED=0
export SLM_RATIO=0.6
export QUANT_MODE=int8_zlib
export USE_GPTQ=0
