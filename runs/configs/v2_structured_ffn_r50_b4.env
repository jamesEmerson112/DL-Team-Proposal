# V2 C6 + Structured FFN (rank=0.5, blocks=4) — Paper #5 test
# Low-rank up-proj (dim->rank->hidden) + block-diagonal down-proj
# ~56% fewer MLP params, expect higher step count in 10 min
export STRUCTURED_FFN=1
export FFN_RANK_RATIO=0.5
export FFN_NUM_BLOCKS=4
export LR_WARMUP_FRAC=0.0
