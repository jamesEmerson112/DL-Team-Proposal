# SLM Validation Runs — 2×H100 RunPod Setup
# These are the FIRST real SLM runs. All previous "SLM" runs were invalid
# (RunPod had commit d7af1ec which predates SLM code).

# === 1. Setup ===
git clone https://github.com/jamesEmerson112/DL-Team-Proposal.git
cd DL-Team-Proposal
git fetch origin
git checkout James-experiment
git pull origin James-experiment

# === 2. Verify SLM code exists ===
# CRITICAL: must be >0 or SLM won't work
grep -c "slm_enabled" parameter-golf/train_gpt.py
# Expected: 3+ matches

# === 3. Install deps ===
cd parameter-golf
python -m pip install --upgrade pip
pip install -r requirements.txt
pip install huggingface_hub
pip install --upgrade torch
python -c "import torch; print(f'PyTorch version: {torch.__version__}')"

# === 4. Download SP8192 dataset (~5-10 min) ===
rm -f data/manifest.json
MATCHED_FINEWEB_REPO_ID=kevclark/parameter-golf python3 data/cached_challenge_fineweb.py --variant sp8192 --train-shards 80

cd ..

# === 5. Run SLM validation (4 runs, ~50 min) ===
bash runs/run_slm_validation_2gpu.sh

# === Expected output ===
# S1: SP1024 GQA + SLM k=0.6   → compare to 1.2649 (Run 6v2)
# S2: SP8192 combo + SLM k=0.6 → compare to 1.2411 (Run A)
# S3: SP8192 combo + SLM k=0.7 → compare to 1.2411 (Run A)
# S4: SP8192 combo + SLM k=0.8 → compare to 1.2411 (Run A)
# Winner (lowest BPB among S2-S4) → goes to 8×H100
