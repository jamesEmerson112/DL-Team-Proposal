python3 -c "
from huggingface_hub import hf_hub_download
import json
cached = hf_hub_download(
    repo_id='romeerp/parameter-golf-caseops-v1',
    filename='manifest.json',
    subfolder='datasets',
    repo_type='dataset',
)
manifest = json.loads(open(cached).read())
print(json.dumps(manifest, indent=2))
"
