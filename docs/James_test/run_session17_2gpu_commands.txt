# =============================================================================
# Session 17: DiffAttn A/B Test on 2×H100 (2 runs)
# =============================================================================
#
# N1: C6 + EMA=0.990 + Small Batch (legal combo baseline)
# N2: N1 + Differential Attention (Paper #19)
#
# HybridNorm (N3) CUT — teammate Ashray tested on rank 4 stack, +0.011 regression.
#
# Estimated time: ~26 min total (2 runs × ~13 min each)
# Estimated cost: ~$1 for 2×H100 pod (~30 min)

# =============================================================================
# STEP 0: SETUP (RunPod 2×H100)
# =============================================================================

git clone https://github.com/jamesEmerson112/DL-Team-Proposal.git
cd DL-Team-Proposal
git fetch origin
git checkout James-experiment-3
git pull origin James-experiment-3

# === Install deps ===
cd parameter-golf
python -m pip install --upgrade pip
pip install -r requirements.txt
pip install huggingface_hub brotli
pip install --upgrade torch
python -c "import torch; print(f'PyTorch version: {torch.__version__}')"

# Install FlashAttention-3 (required — NOT the same as flash-attn pip package)
python3 -c "import torch; print(f'CUDA: {torch.version.cuda}')"
# CUDA 13.0 wheel:
pip install --no-cache-dir \
  "https://download.pytorch.org/whl/cu130/flash_attn_3-3.0.0-cp39-abi3-manylinux_2_28_x86_64.whl"

# === Download SP8192 dataset (~5-10 min) ===
rm -f data/manifest.json
MATCHED_FINEWEB_REPO_ID=kevclark/parameter-golf python3 data/cached_challenge_fineweb.py --variant sp8192 --train-shards 80

# === Preflight check ===
python3 -c "
from flash_attn_interface import flash_attn_func as fa3
import brotli
import torch
print(f'PyTorch: {torch.__version__}')
print(f'CUDA: {torch.version.cuda}')
print(f'GPUs: {torch.cuda.device_count()}')
print(f'FA3: OK')
print(f'brotli: OK')
assert torch.cuda.device_count() >= 2, f'Need 2 GPUs, found {torch.cuda.device_count()}'
print('Preflight PASSED')
"

cd ..  # back to DL-Team-Proposal root

# =============================================================================
# STEP 1: RUN ALL EXPERIMENTS
# =============================================================================
# Runs N1→N2 sequentially, prints summary at end.

cd parameter-golf
bash ../runs/run_v2_session17_2gpu.sh
cd ..

# =============================================================================
# EXPECTED RESULTS
# =============================================================================
#
# N1 (combo baseline): ~1.135 BPB (EMA=0.990 gives -0.012, SmallBatch gives -0.015)
# N2 (DiffAttn): ? — two-softmax-subtract, 2× attn compute, may lose steps
#
# Compare against:
#   R3 (EMA=0.990 only):     1.1505 BPB
#   B2 (SmallBatch only):    1.1419 BPB
#   C6 (no tuning):          1.1622 BPB
